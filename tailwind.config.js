// tailwind.config.js (in your project root, same level as package.json)
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Tells Tailwind which files to scan for class names
  theme: { extend: {} },
  plugins: [],
}
